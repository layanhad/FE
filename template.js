const name = "layan";
const age = 24;
console.log(`My name is ${name} and I am ${age} years old.`);

const length = 10;
const width = 18;
const area = length * width ;
console.log(`The area of a rectangle with length of ${length} and width of ${width} is ${area} .`);

const today = "Wednesday";
const task1 = "attend the zoom class";
const task2 = "finish my homework";
const task3 = "go to the gym";
console.log(`On ${today}, I will ${task1}, ${task2}, ${task3}.`);

const numberOfPets = 1;
console.log(`I have ${numberOfPets} pets.`);

const base = 5; 
const height = 10;
const calculatedArea = (base * height) / 2;
console.log(`The area of a triangle with base ${base} and height ${height} is ${calculatedArea}.`);

